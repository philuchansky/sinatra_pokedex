<h1><%= @title %></h1>
<ul class="list-group">
	<% @pokemon.each do |p| %>
		<li class="list-group-item">
			<img class="poke-thumb" src="<%= p['img_url'] %>">
			<span><%= p['name'] %></span>
		</li>
	<% end %>
</ul>